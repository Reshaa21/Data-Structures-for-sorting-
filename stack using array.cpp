#include <stdio.h>
 intdata;
 struct node{
 struct node* next;
};
struct node* createNode(int data){
	struct Node* newNode = (struct Node*) malloc(size of(struct Node));
	if(newNode== NULL){
		printf
	}
} 
